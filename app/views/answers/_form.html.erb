<%= form_for([@question, @question.answers.build]) do |f| %>
  <p>
    <%= f.label :description, 'Add an answer:' %><br>
    <%= f.text_area :description %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
